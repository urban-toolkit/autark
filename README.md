# UTK-v2

This project contains the following sub-projects:
- `utkdb`
- `utkmap`

## Makefile

- **`make install`**: Install dependencies for all projects.
- **`make dev`**: Run the project in development mode.

**Note:** If you do not have `make` installed, run `sudo apt-get install build-essential`.
